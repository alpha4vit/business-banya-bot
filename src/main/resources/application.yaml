spring:
  datasource:
    url: jdbc:mysql://localhost:5432/business_banya_bot_db
    username: postgres
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    url: ${spring.datasource.url}
    user: ${spring.datasource.username}
    password: ${spring.datasource.password}
    driver-class-name: ${spring.datasource.driver-class-name}
  jpa:
    hibernate:
      ddl-auto: validate

application:
  telegram:
    token: 8030327375:AAGLyLpQSn-Yhx7i7C2s8S7Cl-TQTttz2go